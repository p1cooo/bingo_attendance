rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All Academy records are accessed through the verified server API.
    // Firebase Admin bypasses these rules; browser clients do not.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
